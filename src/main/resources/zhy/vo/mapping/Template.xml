<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhy.dao.ITemplateDAO">

    <insert id="insertTemplate" parameterType="Template">
        INSERT INTO template(openid, content, templateType ,state, updateTime) VALUES (#{openid}, #{content}, #{templateType}, 1, #{updateTime})
    </insert>

    <update id="updateTemplate" parameterType="java.lang.String">
        UPDATE template
        SET state=0
        WHERE id=#{id}
    </update>

    <select id="selectTemplate" parameterType="java.lang.String" resultType="Template">
        SELECT id, content, templateType FROM template WHERE openid=#{openid} AND state=1
    </select>

</mapper>